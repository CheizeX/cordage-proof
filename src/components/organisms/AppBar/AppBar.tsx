import { StyledAppBar } from "./AppBar.styles";
import IconButton from "../../molecules/IconButton/IconButton";

const Navbar = () => {
  return (
    <StyledAppBar>
      <IconButton icon='/vite.svg' />
    </StyledAppBar>
  );
};

export default Navbar;
